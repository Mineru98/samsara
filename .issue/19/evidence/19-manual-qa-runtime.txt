Manual QA runtime transcript for issue #19 cache-integrity fix
Branch: fix/19-onboard-cache-integrity
Implementation commit: 8900cb9
Base: origin/main b9e0277e61dc56eabb5e7a19851169ef44943261
Worktree: /Users/mineru/SourceCode/samsara-issue-19

S1 surface=terminal/Node test runner
Invocation: node --test skills/issue-onboard/scripts/*.test.mjs
Observed: tests 62; pass 62; fail 0; skipped 0; todo 0; exit 0.
Observed markers: FINAL_CACHE_VALIDATION=changed-after-load EXIT=1 RECOMMENDATION=none; OUTPUT_CACHE_VALIDATION=changed-during-emission EXIT=1 RECOMMENDATION=none; OUTPUT_CACHE_LOCK=official-writer-blocked EXIT=0 RECOMMENDATION=stable.

S2 surface=terminal/Node test runner
Invocation: node --test tools/issue-ontology/ontology.test.mjs
Observed: tests 13; pass 13; fail 0; skipped 0; todo 0; exit 0.

S3 surface=terminal/Node syntax checker
Invocation: node --check skills/issue-onboard/scripts/issue-onboard.mjs; node --check skills/issue-onboard/scripts/bootstrap.test.mjs; node --check skills/issue-onboard/scripts/issue-common.mjs; node --check skills/issue-create/scripts/issue-common.mjs; node --check skills/issue-start/scripts/issue-common.mjs; node --check skills/issue-end/scripts/issue-common.mjs; node --check skills/issue-merge/scripts/issue-common.mjs; node --check skills/issue-sync/scripts/issue-common.mjs
Observed: all eight commands exit 0; git diff --check exit 0.

S4 surface=terminal/Node targeted regression runner
Invocation: node --test --test-name-pattern='reports invalid caches and fails when bootstrap fails|onboarding CLI bootstraps a complete sync|graph cache changes after the final graph load|graph cache changes during recommendation output|holds the cache lock|graph writers fail closed|reclaims a cache lock|restores a live lock replaced before stale-lock cleanup|all graph writers reject symlinked cache paths|all graph writers reject hard-linked cache files|all graph writers reject a symlink swap at the final open|all graph writers reject a parent-directory symlink swap at the final open|saveGraph rejects a parent-directory swap at the final rename|all graph writers reject a final-file symlink swap before replacement' skills/issue-onboard/scripts/bootstrap.test.mjs
Observed: tests 14; pass 14; fail 0; skipped 0; todo 0; exit 0.
Observed markers: CACHE_VALIDATION=malformed EXIT=7 RECOMMENDATION=none; CACHE_VALIDATION=schema-invalid EXIT=7 RECOMMENDATION=none; FINAL_CACHE_VALIDATION=changed-after-load EXIT=1 RECOMMENDATION=none; OUTPUT_CACHE_VALIDATION=changed-during-emission EXIT=1 RECOMMENDATION=none; OUTPUT_CACHE_LOCK=official-writer-blocked EXIT=0 RECOMMENDATION=stable.

S5 surface=terminal/CLI subprocess fixture
Observed: normal bootstrap still emits ONBOARD_COUNT=1; partial/failed, malformed, schema-invalid, stale, live-read, final-load, and symlinked-writer cases do not unlock an unsafe recommendation or external write.

S6 surface=terminal/CLI subprocess plus official-writer subprocess
Observed: during final recommendation validation/output, a saveGraph writer exits 1 on the shared lock; the onboarding command exits 0, emits a stable recommendation, and leaves graph.json unchanged. A valid lock with a dead owner PID is reclaimed, while malformed or live-owner locks remain blocking. When a replacement lock is installed immediately before stale-lock cleanup renames the old lock, all six writers restore it at the canonical path and a second writer remains blocked. All six patchGraphNode copies reject symlinked and hard-linked cache paths, final-open and final-replacement symlink swaps, and parent-directory swaps, leaving the outside graph unchanged. `saveGraph` rejects a parent swap during final rename without overwriting the outside graph or temporary file and cleans the old-parent lock/temp files.

S7 surface=terminal/git worktree inspection
Invocation: git status --short --untracked-files=no; git diff --check origin/main...HEAD
Observed: no tracked worktree changes; diff check exit 0. `.local/` remains untracked by explicit preservation decision and was not read or modified.
