# Before: GLM ACP integration state

- Root `AGENTS.md` was absent.
- The repository contained nine workflow `SKILL.md` files and four Markdown
  agent playbooks.
- There was no `commands/` directory or GLM ACP command map.
- `glm-acp-agent` was installed locally, but no repository project context
  linked its ACP sessions to the SAMSARA workflow.
